<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="navbar flex justify-between w-full px-20 py-10">
        
        <%var loggedin=typeof loggedin!=='undefined'?loggedin:true %>
        <% if(loggedin){ %>
            <h3>Khaatabook</h3>
        <% }%>
        <% var errorMessages = (typeof message !== 'undefined' && message.error && message.error.length > 0) ? message.error : []; %>

        <% if (errorMessages.length > 0) { %>
            <div class="absolute top-10 left-1/2 -translate-x-1/2 -translate-y-1/2 px-3 py-2 rounded-md " style="background-color: #FFC000">
                <h3 class="text-white">
                    <%= errorMessages[0] %>
                </h3>
            </div>
        <% } %>
        
        <% if(loggedin){ %>
            <div class="links flex gap-4">
                <a href="/">Home</a>
                <a href="/hisaab/create">Create new hisaab</a>
                <a class="text-red-500" href="/logout">Logout</a>
            </div>
            <% }%>
    </div>